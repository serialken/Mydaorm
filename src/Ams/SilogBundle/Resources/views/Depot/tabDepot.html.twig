{% import '::macros.html.twig' as m %}
<div class="depot tab-pane fade {{ active is defined ? active : '' }}" id="depot_{{ depot.id }}">
    <div class="" style="text-align:left;">
        <h1 class="page-header" style="text-align:left;">Dépôt {{ depot.libelle }}</h1>
    </div>
    <div class="" style="text-align:left;">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Code</th>
            <th>Nom</th>
            <th>Adresse</th>
            <th>Code Postal</th>
            <th>Ville</th>
            <th>Code INSEE</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody id="details_depot_{{ depot.id}}">
             <tr>
                <td>{{ depot.code }}</td>
                <td>{{ depot.libelle }}</td>
                <td>{{ depot.adresse }}</td>
                <td>{{ depot.commune.cp }}</td>
                <td>{{ depot.commune.libelle }}</td>
                <td>{{ depot.commune.insee }}</td>
                <td><a href="#" data-toggle="modal" data-target="#amsModal" onClick="ChargeModal('{{ path('depot_modification_ajax') }}',{{depot.id}});"><span class="glyphicon glyphicon-edit"></span> Modifier</a></td>
            </tr>
        </tbody>
      </table>
    </div>
    <div class="" style="text-align:left;">
    <h2 class="sub-header" style="text-align:left;">Villes associées au dépôt </h2>
    {#<a href="#" data-toggle="modal" data-target="#amsModal" onClick="ChargeModal('{{ path('depot_ajout_commune_ajax') }}',{{depot.id}});"><span class="glyphicon glyphicon-edit"></span> Ajouter commune</a>#}
    </div>
        <div  style='position:relative; height:700px; width:100%;'>
            <table width="100%" cellpadding="0" cellspacing="0">
                <tr>
                    <td>
                        {#<a class="editlink" href="#"  onClick="gridCommune.toExcel({{m.urlExcel()}},'color',true);showModalFlashAlert('info', 'Veuillez patienter pendant la génération du fichier excel.', 25000);" data-description="Export au format Excel" data-content="Ce lien permet d'exporter les données présentent dans le tableau au format (.xls)." onmouseover="affPopoverLien(this);" ><span class="glyphicon glyphicon-edit"></span> Exporter </a>#}
                        <img style="float:right; margin:3px; cursor:pointer" src="{{ asset('images/excel.png') }}" alt="Imprimer"  onclick="gridCommune.toExcel({{m.urlExcel()}},'color',true);" >
                        <div id="gridbox_{{depot.id}}"  style="width:100%; height:500px; background-color:white;overflow:hidden"></div>
                        <div id="cover_{{depot.id}}" style='width:500px; font-size:20pt; text-align:center; font-family:Tahoma; position:absolute; top:50px; left:100px; height:150px; widht:500px; background-color:silver; opacity:0.3; -moz-opacity:0.3; filter:alpha(opacity=30);'>
                            Chargement ...
                        </div>
                    </td>
                </tr>
                <tr>
                    <td>
                        <div id="recinfoArea"></div>
                    </td>
                </tr>
            </table>  
        </div>
</div>
      